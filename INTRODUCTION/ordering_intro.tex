The following section discusses \openshmem \ac{API} that provides a mechanism to ensure ordering of that provides a mechanism to ensure ordering of \OPR{Put}, \acp{AMO}, and memory store operations to symmetric data 
objects. 